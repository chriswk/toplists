<h2>Edit Country</h2>

<%= render "form.html", changeset: @changeset,
                        action: country_path(@conn, :update, @country) %>

<span><%= link "Back", to: country_path(@conn, :index) %></span>
